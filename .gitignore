__pycache__/
*.pyc
*.pyo
.venv/
.env
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Instance state lives with the instance (the site repo) under state.root, never
# in the engine. Since v0.4.1 the tool ships stateless: if the engine is run from
# a checkout of this repo, these dirs are created at runtime and stay untracked.
raw/
memory/
provenance/
drafts/
approved/
published/
